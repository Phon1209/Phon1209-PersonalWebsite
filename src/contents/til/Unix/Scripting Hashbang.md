It’s also called `shebang` for some reason, I don’t know why (don’t ask me).

I was assuming that we need this for every script file without questioning why. But recently, I just learned that `#!/bin/bash` is for telling which kind of interpreter to run the script.

Giving this function, I can also put `#!/usr/bin/python3` and use the script like it’s python file as well. Although it’s not that useful since we can just use `.py` file